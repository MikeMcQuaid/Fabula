IF(APPLE OR WIN32)
	file(INSTALL DESTINATION "${CMAKE_INSTALL_PREFIX}/@PLUGINS_DIR@/sqldrivers" TYPE FILES "@QT_SQL_PLUGINS@")
	file(GLOB QT_SQL_PLUGINS_ALL "${CMAKE_INSTALL_PREFIX}/@PLUGINS_DIR@/sqldrivers/*")
endif()

if(APPLE)
	file(WRITE "${CMAKE_INSTALL_PREFIX}/@EXECUTABLE@/Contents/Resources/qt.conf" "[Paths]\nPlugins = @PLUGINS_DIR_ONLY@")
endif()

include("${CMAKE_ROOT}/Modules/BundleUtilities.cmake")
set(BU_CHMOD_BUNDLE_ITEMS TRUE)
fixup_bundle("${CMAKE_INSTALL_PREFIX}/@EXECUTABLE@" "${QT_SQL_PLUGINS_ALL}" "@QT_LIBRARY_DIR@")
